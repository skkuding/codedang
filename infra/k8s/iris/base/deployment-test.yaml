apiVersion: apps/v1
kind: Deployment
metadata:
  name: iris-test
  namespace: iris
spec:
  replicas: 1
  selector:
    matchLabels:
      app: iris-test
  template:
    metadata:
      labels:
        app: iris-test
    spec:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: iris-test
      containers:
        - name: iris
          image: ghcr.io/skkuding/codedang-iris
          resources:
            limits:
              cpu: 1
              memory: 1.5Gi
            requests:
              cpu: 1
              memory: 1.5Gi
          volumeMounts:
            - name: cgroup
              mountPath: /sys/fs/cgroup
              readOnly: false
          securityContext:
            privileged: true
          envFrom:
            - configMapRef:
                name: iris-env
            - secretRef:
                name: aws-credentials
            - secretRef:
                name: database-credentials
          env:
            - name: JUDGE_REQUEST_CONSUMER_CONNECTION_NAME
              value: iris-test-consumer
            - name: JUDGE_REQUEST_QUEUE_NAME
              value: client.q.judge.test
            - name: JUDGE_REQUEST_CONSUMER_TAG
              value: iris-test-consumer-tag
            - name: RABBITMQ_DEFAULT_USER
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-credentials
                  key: username
            - name: RABBITMQ_DEFAULT_PASS
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-credentials
                  key: password
      volumes:
        - name: cgroup
          hostPath:
            path: /sys/fs/cgroup
            type: Directory
