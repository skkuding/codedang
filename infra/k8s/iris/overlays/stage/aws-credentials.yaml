---
# Reflector consumer: copies secret from minio/iris-user
# SSoT: credentials are managed in infra/k8s/minio/users/iris.yaml
apiVersion: v1
kind: Secret
metadata:
  name: aws-credentials
  namespace: iris
  annotations:
    reflector.v1.k8s.emberstack.com/reflects: 'minio/iris-user'
