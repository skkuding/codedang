---
# Reflector consumer: copies secret from minio/plag-user
# SSoT: credentials are managed in infra/k8s/minio/users/plag.yaml
apiVersion: v1
kind: Secret
metadata:
  name: aws-credentials
  namespace: plag
  annotations:
    reflector.v1.k8s.emberstack.com/reflects: 'minio/plag-user'
