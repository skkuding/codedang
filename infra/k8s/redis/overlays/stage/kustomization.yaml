apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: redis

resources:
  - ../../base

patches:
  - path: credentials.yaml
    target:
      kind: SealedSecret
      name: redis-credentials
