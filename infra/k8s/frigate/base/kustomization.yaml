apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: frigate

resources:
  - namespace.yaml
  - configmap.yaml
  - pvc-config.yaml
  - pvc-media.yaml
  - deployment.yaml
  - service.yaml

commonAnnotations:
  managed-by: kustomize