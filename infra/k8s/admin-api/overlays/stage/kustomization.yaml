apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: admin-api

resources:
  - ../../base
  - cors-middleware.yaml
  - secrets/database-credentials.yaml
  - secrets/aws-credentials.yaml
  - secrets/security-keys.yaml

patches:
  - path: configmap.yaml
    target:
      kind: ConfigMap
      name: admin-api-env
  - path: HTTPRoutes.yaml
    target:
      kind: HTTPRoute
      name: admin-api-server-gateway
  - path: TLSRedirect.yaml
    target:
        kind: HTTPRoute
        name: admin-api-tls-redirect
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: admin-api
