apiVersion: v1
kind: Service
metadata:
  name: admin-api-server
  namespace: admin-api
  labels:
    app: admin-api
spec:
  selector:
    app: admin-api
  ports:
    - name: http
      port: 3000
      targetPort: 3000
