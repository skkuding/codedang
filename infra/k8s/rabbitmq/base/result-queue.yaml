apiVersion: rabbitmq.com/v1beta1
kind: Queue
metadata:
  name: result-queue
  namespace: rabbitmq
spec:
  name: iris.q.judge.result
  vhost: vh
  autoDelete: false
  durable: true
  rabbitmqClusterReference:
    name: rabbitmq
