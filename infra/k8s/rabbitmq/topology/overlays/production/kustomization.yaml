apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: admin-credentials.yaml
    target:
      kind: SealedSecret
      name: admin-credentials
