apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: frontend

resources:
  - configmap.yaml
  - deployment.yaml
  - HTTPRoutes.yaml
  - service.yaml
  - ingress.yaml

commonAnnotations:
  managed-by: kustomize
