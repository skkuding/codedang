apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cloud-beaver-pvc
  namespace: cloud-beaver
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path
