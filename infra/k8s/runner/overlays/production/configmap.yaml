apiVersion: v1
kind: ConfigMap
metadata:
  name: pod-manager-env
  namespace: runner
data:
  RUNNER_IMAGE_TAG: 'production'
  RUNNER_POOL_SIZE: '60'
  RUNNER_REDIS_PREFIX: 'codedang:runner:production'
