meta {
  name: Succeed
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/contest/auth
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

script:pre-request {
  await require("./login").loginUser(req);
}
