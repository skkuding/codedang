meta {
  name: Succeed
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/contest/1/statistics/problem/1
  body: none
  auth: inherit
}

script:pre-request {
  await require("./login").loginUser(req);
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  ## 📘 Get Statistics By Problem
  
  **GET** `/contest/:id/statistics/problem/:problemId`
  
  특정 대회 문제에 해당하는 통계를 반환합니다.
  
  
  ### 🔒 Authentication
  
  종료되지 않은 대회의 경우 403 에러를 반환합니다.
  
  ### 📥 Request Parameters
  
  #### Path Parameters
  
  | Name      | Type   | Required | Description                      |
  |-----------|--------|----------|----------------------------------|
  | `id`      | number | ✅       | ID of the target contest         |
  | `problemId`      | number | ✅       | ID of the target problem         |
  
  #### Response Body
  
  | Name      | Type  | Description                |
  |-----------|-------|----------------------------------|
  | `totalSubmissionCount`| number   | 전체 제출 수 |
  | `acceptedSubmissionCount` | number| 정답 제출 수 |
  | `acceptedRate` | float(소수 첫째자리) | 정답률(제출 수 기준) |
  | `averageTrial` | float(소수 첫째자리) | 평균 시도 횟수 |
  | `firstSolver` | {id: number, username: string} | 최단 정답자(제출시간 기준) |
  | `fastestSolver` | {id: number, username: string} | 최단 정답자(cpuTime 기준) |
  | `userSpeedRank` | number or null | 요청 유저의 제출시간 등수(대회에 참가하지 않거나 정답을 맞추지 못한 경우 null) |
  | `acceptedSubmissionsByLanguage` | {language: string, count : number} | 언어별 정답 제출 수 |
  
  
  
  
}
