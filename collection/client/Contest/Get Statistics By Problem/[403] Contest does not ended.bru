meta {
  name: [403] Contest does not ended
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/contest/2/statistics/problem/1
  body: none
  auth: inherit
}

script:pre-request {
  await require("./login").loginUser(req);
}

settings {
  encodeUrl: true
  timeout: 0
}
