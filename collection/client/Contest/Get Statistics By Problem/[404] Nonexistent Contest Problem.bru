meta {
  name: [404] Nonexistent Contest Problem
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/contest/1/statistics/problem/999
  body: none
  auth: inherit
}

script:pre-request {
  await require("./login").loginUser(req);
}

settings {
  encodeUrl: true
  timeout: 0
}
