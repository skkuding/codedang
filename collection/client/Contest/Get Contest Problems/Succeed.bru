meta {
  name: Succeed
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/contest/1/statistics/problems
  body: none
  auth: inherit
}

script:pre-request {
  await require("./login").loginUser(req);
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  ## 📘 Get Contest Problems
  
  **GET** `/contest/:id/statistics/problems`
  
  특정 대회의 problem 목록을 반환합니다.
  
  
  ### 🔒 Authentication
  
  종료되지 않은 대회의 경우 403 에러를 반환합니다.
  
  ### 📥 Request Parameters
  
  #### Path Parameters
  
  | Name      | Type   | Required | Description                      |
  |-----------|--------|----------|----------------------------------|
  | `id`      | number | ✅       | ID of the target contest         |
  
  #### 
  
  
  
}
