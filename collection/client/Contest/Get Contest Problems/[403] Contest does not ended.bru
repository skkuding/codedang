meta {
  name: [403] Contest does not ended
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/contest/2/statistics/problems
  body: none
  auth: inherit
}

script:pre-request {
  await require("./login").loginUser(req);
}

settings {
  encodeUrl: true
  timeout: 0
}
