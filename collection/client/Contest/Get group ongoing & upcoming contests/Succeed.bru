meta {
  name: Succeed
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/group/2/contest
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

script:pre-request {
  await require("./login").loginUser(req);
}
