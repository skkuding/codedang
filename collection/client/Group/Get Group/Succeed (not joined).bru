meta {
  name: Succeed (not joined)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/group/3
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res("id"): eq 3
  res("groupName"): isString
  res("description"): isString
  res("allowJoinFromSearch"): isBoolean
  res("memberNum"): isNumber
  res("leaders"): isDefined
  res("isJoined"): eq false
}

script:pre-request {
  await require("./login").loginUser(req);
}
