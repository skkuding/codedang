meta {
  name: [404] Nonexistent Course
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/course/9999/qna/1
  body: json
  auth: none
}

body:json {
  {
    "title": "Updated title"
  }
}

script:pre-request {
  const login = require('./login');
  await login.loginUser(req);
}

tests {
  test("should return 404 Not Found", function() {
    expect(res.getStatus()).to.equal(404);
  });
}

docs {
  # 📘 Update Question - Course Not Found

  존재하지 않는 강의 ID로 질문을 수정하려고 하면 404 Not Found 에러가 발생합니다.
}
