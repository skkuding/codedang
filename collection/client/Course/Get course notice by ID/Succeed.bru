meta {
  name: Succeed
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/course/1/notice/1
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res("current").title: isString
  res("current").content: isString
  res("current").createTime: isString
  res("current").updateTime: isString
  res("current").createdBy: isString
  res("prev"): isDefined
  res("next"): isDefined
}

script:pre-request {
  await require("./login").loginUser(req);
}

docs {
  # 📘 Get a Course Notice By ID

  **GET** `/course/:groupId/notice/:id`

  Retrieve a single QnA for a specific contest.

  강의 공지사항을 1개 조회합니다.

  이 함수는 다음과 같은 조건에 따라 강의 공지사항을 조회합니다.

  1. 운영진(Admin, Manager)
  - 모든 공지를 열람할 수 있습니다.
  3. 일반 사용자
   - 전체 공개인 공지를 열람할 수 있습니다.
   - 자신이 참여한 강의의 공지를 열람할 수 있습니다.

  접근 권한이 없는 경우 예외를 발생시킵니다.

  - 일반 사용자: 본인이 참여하지 않은 강의의 공지사항의 경우 ForbiddenAccessException 발생

  ### 🔒 Authentication

  ### 📥 Request Parameters

  #### Path Parameters

  | Name | Type   | Required | Description              |
  |------|--------|----------|--------------------------|
  | `groupId` | number | ✅    | 공지가 속한 강의 아이디 |
  | `id` | number | ✅    | 가져오고자 하는 공지 아이디 |
}
