meta {
  name: [404] Nonexistent Course
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/course/9999/qna
  body: json
  auth: none
}

body:json {
  {
    "title": "Nonexistent course test",
    "content": "This should fail."
  }
}

script:pre-request {
  const login = require('../../../../login');
  await login.loginUser(req);
}

tests {
  test("should return 404 Not Found", function() {
    expect(res.getStatus()).to.equal(404);
  });
}

docs {
  # 📘 Create Course Question - Course Not Found

  존재하지 않는 강의 ID로 질문을 작성하려고 하면 404 Not Found 에러가 발생합니다.
}
