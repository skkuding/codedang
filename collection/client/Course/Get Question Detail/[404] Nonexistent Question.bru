meta {
  name: [404] Nonexistent Question
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/course/1/qna/9999
  body: none
  auth: none
}

script:pre-request {
  const login = require('./login');
  await login.loginUser(req);
}

tests {
  test("should return 404 Not Found", function() {
    expect(res.getStatus()).to.equal(404);
  });
}

docs {
  # 📘 Get Question Detail - Question Not Found

  존재하지 않는 질문 order로 조회하려고 하면 404 Not Found 에러가 발생합니다.
}
