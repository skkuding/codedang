meta {
  name: [404] Nonexistent Course
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/course/9999/qna/1/comment/1
  body: none
  auth: none
}

script:pre-request {
  const login = require('./login');
  await login.loginUser(req);
}

settings {
  encodeUrl: true
}

tests {
  test("should return 404 Not Found", function() {
    expect(res.getStatus()).to.equal(404);
  });
}

docs {
  # 📘 Delete Comment - Course Not Found

  존재하지 않는 강의 ID로 댓글을 삭제하려고 하면 404 Not Found 에러가 발생합니다.
}
