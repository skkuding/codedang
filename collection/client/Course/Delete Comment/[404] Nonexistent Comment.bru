meta {
  name: [404] Nonexistent Comment
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/course/1/qna/1/comment/9999
  body: none
  auth: none
}

script:pre-request {
  const login = require('./login');
  await login.loginUser(req);
}

settings {
  encodeUrl: true
}

tests {
  test("should return 404 Not Found", function() {
    expect(res.getStatus()).to.equal(404);
  });
}

docs {
  # 📘 Delete Comment - Comment Not Found

  존재하지 않는 댓글 order로 삭제하려고 하면 404 Not Found 에러가 발생합니다.
}
