meta {
  name: [404] Nonexistent Question
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/course/1/qna/9999/comment/1
  body: none
  auth: none
}

script:pre-request {
  const login = require('./login');
  await login.loginUser(req);
}

settings {
  encodeUrl: true
}

tests {
  test("should return 404 Not Found", function() {
    expect(res.getStatus()).to.equal(404);
  });
}

docs {
  # 📘 Delete Comment - Question Not Found

  존재하지 않는 질문 order에서 댓글을 삭제하려고 하면 404 Not Found 에러가 발생합니다.
}
