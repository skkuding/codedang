meta {
  name: [404] Nonexistent Course
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/course/9999/qna/1/comment
  body: json
  auth: none
}

body:json {
  {
    "content": "Comment on nonexistent course"
  }
}

script:pre-request {
  const login = require('../../../../login');
  await login.loginUser(req);
}

settings {
  encodeUrl: true
}

tests {
  test("should return 404 Not Found", function() {
    expect(res.getStatus()).to.equal(404);
  });
}

docs {
  # 📘 Create Comment - Course Not Found

  존재하지 않는 강의 ID로 댓글을 작성하려고 하면 404 Not Found 에러가 발생합니다.
}
