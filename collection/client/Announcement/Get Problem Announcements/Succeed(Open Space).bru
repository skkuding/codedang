meta {
  name: Succeed(Open Space)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/announcement?problemId=1
  body: none
  auth: none
}

query {
  problemId: 1
}

assert {
  res("id")[0]: isNumber
  res("content")[0]: isString
  res("problemId")[0]: isNumber
}
