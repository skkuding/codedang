meta {
  name: [Succeed] Assignment(WA)
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/submission?problemId=1&assignmentId=1
  body: json
  auth: none
}

params:query {
  problemId: 1
  assignmentId: 1
  ~workbookId: 1
}

body:json {
  {
    "code": [
      {
        "id": 1,
        "text": "#include <stdio.h>\nint main() { int a, b; scanf(\"%d%d\", &a, &b); printf(\"%d\\n\", a - b);}",
        "locked": false
      }
    ],
    "language": "C"
  }
}

assert {
  res.body.id: isNumber
  res.body.userId: isNumber
  res.body.problemId: isNumber
  res.body.assignmentId: isNumber
  res.body.workbookId: isNull
  res.body.code: isArray
  res.body.code[0]: isJson
  res.body.codeSize: isNumber
  res.body.language: isString
  res.body.result: isString
  res.body.createTime: isString
  res.body.updateTime: isString
}

script:pre-request {
  await require("./login").loginUser(req);
}

docs {
  ## Create Submission (WA)
  
  `Assignment`에 WA 판정을 받는 코드를 제출하여, 채점 요청을 보냅니다.
  
  ---
  
  ### 🔒 Authentication
  
  ✅ Required  
  
  ---
  
  ### 📥 Request Parameters
  
  #### Query Parameters
  | Name | Type   | Required | Description              |
  |------|--------|----------|--------------------------|
  | `problemId` | number |   ✅   | id of problem |
  | `contestId` | number |        | id of contest |
  | `assignmentId` | number |        | id of assignment |
  | `workbookId` | number |        | id of workbook |
  
  
  
  ---
  
  ### 📝 Request Body
  
  #### Content-Type: `application/json`
  
  ```json
  {
    "code": [
      {
        "id": 1,
        "text": "#include <stdio.h>\nint main() { int a, b; scanf(\"%d%d\", &a, &b); printf(\"%d\\n\", a + b);}",
        "locked": false
      }
    ],
    "language": "C"
  }
  ```
  
  | Field | Type   | Required | Description              |
  |------|--------|----------|--------------------------|
  | `code` | array | ✅       | 제출 코드의 정보 |
  | `id` | int | ✅       | 코드의 id |
  | `text` | string | ✅       | 소스 코드의 내용 |
  | `locked` | boolean | ✅       | 코드의 잠금상태 여부 |
  | `language` | string | ✅       |  제출 코드의 언어 |
  
  ### 📝 Response Body
  
  #### Content-Type: `application/json`
  
  ```json
  {  
  "code": [
      {
        "id": 1,
        "text": "#include <iostream>\nint main() { return 0; }",
        "locked": false
      }
    ],
    "result": "Judging",
    "userId": 77,
    "userIp": "203.0.113.10",
    "problemId": 1,
    "codeSize": 43,
    "language": "cpp",
    "assignmentId": 1
  }
  
  
  ```
  
  | Field | Type   | Required | Description              |
  |------|--------|----------|--------------------------|
  | `code` | array | ✅       | 제출 코드의 정보 |
  | `id` | number | ✅       | 코드의 id |
  | `text` | string | ✅       | 소스 코드의 내용 |
  | `locked` | boolean | ✅       | 코드의 잠금상태 여부 |
  | `result` | array | ✅       | 제출 코드의 결과 |
  | `userId` | number | ✅       | user의 Id |
  | `userIp` | string | ✅       | user의 Ip |
  | `problemId` | number | ✅       | 문제의 Id |
  | `language` | string | ✅       |  제출 코드의 언어 |
  | `assignmentId` | number | ✅       | assignment의 Id |
}
