meta {
  name: Succeed(User)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "user01",
    "password": "Useruser"
  }
}

vars:post-response {
  jwtToken: res.headers.authorization
}

assert {
  res.status: eq 201
  res.headers.authorization: contains Bearer
}
