meta {
  name: Succeed
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/group/2/workbook?take=10
  body: none
  auth: none
}

query {
  take: 10
  ~cursor: 6
}

assert {
  res.status: eq 200
  res.body[0].id: isNumber 
  res.body[0].title: isString 
  res.body[0].description: isString 
  res.body[0].updateTime: isString 
}

script:pre-request {
  await require("./login").loginUser(req);
}
