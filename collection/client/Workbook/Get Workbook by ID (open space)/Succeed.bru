meta {
  name: Succeed
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/workbook/1
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res("id"): isNumber
  res("title"): isString
  res("problems")[0].id: isNumber
  res("problems")[0].title: isString
}
