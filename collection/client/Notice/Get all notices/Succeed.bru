meta {
  name: Succeed
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/notice?take=5
  body: none
  auth: none
}

params:query {
  take: 5
  ~cursor: 1
  ~fixed: true
  ~search: 더
}

assert {
  res.status: eq 200
  res.body.data[0].id: isNumber
  res.body.data[0].title: isString
  res.body.data[0].createTime: isString
  res.body.data[0].isFixed: isBoolean
  res.body.data[0].createdBy: isString
}

docs {
  # 📘 Get Notices
  
  **Get** `/notice`
  
  Get a list of notices with pagination and search functionality.
  
  공개된 공지사항들을 `take`개수만큼 가져옵니다.
  
  1. 가시성 및 권한
  
  - `isVisible: true` 상태인 공지사항만 노출됩니다.
  
  - `GroupMemberGuard`를 통해 해당 그룹의 멤버 권한을 가진 사용자만 접근할 수 있습니다.
  
  2. 필터링 및 검색
  
  - fixed: 상단 고정된 공지사항(`true`) 또는 일반 공지사항(`false`)을 선택하여 조회할 수 있습니다.
  
  - search: 제목에 특정 키워드가 포함된 공지사항을 대소문자 구분 없이(`insensitive`) 검색합니다.
  
  3. 데이터 구성
  
  - 커서(`cursor`)를 기준으로 다음에 올 데이터를 지정된 개수(`take`)만큼 가져옵니다.
  
  - 각 게시글 데이터에는 작성자의 이름(`createdBy: username`)이 포함됩니다.
  
  - 조건에 맞는 전체 게시글 수(`total`)를 데이터와 함께 반환합니다.
  
  ---
  
  ### 🔒 Authentication
  
  ✅ Required  
  `GroupMemberGuard`가 적용되어 있어, 유효한 그룹 멤버 권한이 필요합니다. 다만 `@AuthNotNeededIfPublic()` 설정에 따라 프로젝트의 공용 설정에 영향을 받을 수 있습니다.
  
  ---
  
  ### 📥 Request Parameters
  
  #### Query Parameters
  | Name | Type   | Required | Description              |
  |------|--------|----------|--------------------------|
  | `take`  | Integer |      | 가져올 공지사항 개수 (default: 10) |
  | `cursor` | Integer |        | cursor 값 다음의 ID를 가진 공지사항들을 반환 |
  | `fixed` | Boolean |        | 고정된 공지사항 (default: false) |
  | `search` | String |        | 검색 키워드 |
  
  #### Path Parameters
  
  | Name | Type   | Required | Description              |
  |------|--------|----------|--------------------------|
  
  
  ---
  
  ### 📝 Request Body
  
  #### Content-Type: `none`
  
}
