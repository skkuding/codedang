meta {
  name: Succeed
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/notice/1
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res("current").title: isString
  res("current").content: isString
  res("current").createTime: isString
  res("current").updateTime: isString
  res("current").createdBy: isString
  res("prev"): isDefined
  res("next"): isDefined
}

docs {
  # 📘 Get Notice by ID
  
  **GET** `/notice/:id`
  
  Get a specific notice and its neighbors using a unique identifier.
  
  특정 공지사항의 상세 정보와 이전/다음 게시글의 요약 정보를 조회합니다.
  
  1. 가시성 및 조회 조건
  
  - `isVisible: true` 상태인 공지사항만 조회할 수 있습니다.
  
  - 존재하지 않는 ID이거나 비공개 상태인 경우 게시글 요청 시 예외(`findUniqueOrThrow`)가 발생합니다.
  
  2. 목록 이동(이전/이후)
  
  - 현재 게시글을 기준으로 이전 게시글(`prev`)과 다음 게시글(`next`)의 id와 title을 함께 반환합니다.
  
  3. 데이터 가공
  
  - 작성자 정보는 `username` 문자열로 변환하여 응답합니다.
  
  - `GroupMemberGuard`를 통해 해당 그룹에 소속된 사용자만 상세 내용을 확인할 수 있습니다.
  
  ### 🔒 Authentication
  
  ✅ Required  
  `GroupMemberGuard`가 적용되어 있어, 유효한 그룹 멤버 권한이 필요합니다.
  
  ---
  
  ### 📥 Request Parameters
  
  #### Query Parameters
  | Name | Type   | Required | Description              |
  |------|--------|----------|--------------------------|
  | `groupId`  | number |      | 공지사항이 속한 Group ID |
  
  
  #### Path Parameters
  
  | Name | Type   | Required | Description              |
  |------|--------|----------|--------------------------|
  | `id`  | number |   ✅   | 상세 내용을 조회할 공지사항의 고정 ID |
  
  
  ---
  
  ### 📝 Request Body
  
  #### Content-Type: `none`
}
