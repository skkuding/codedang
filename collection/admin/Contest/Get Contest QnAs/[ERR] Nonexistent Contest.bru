meta {
  name: [ERR] Nonexistent Contest
  type: graphql
  seq: 3
}

post {
  url: {{gqlUrl}}
  body: graphql
  auth: none
}

body:graphql {
  query GetContestQnAs($contestId: Int!) {
    getContestQnAs(contestId: $contestId) {
      id
    }
  }
}

body:graphql:vars {
  {
    "contestId": 999
  }
}

script:pre-request {
  await require("./login").loginContestAdmin(req);
}

docs {
  # 📙 [ERR] Nonexistent Contest

  **POST** `/graphql (query: getContestQnAs)`

  The requested contest does not exist.

  존재하지 않는 `contestId`를 포함하여 QnA 목록 조회를 요청했을 때,

  `EntityNotExistException`이 발생합니다.
}
