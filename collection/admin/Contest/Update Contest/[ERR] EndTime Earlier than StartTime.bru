meta {
  name: [ERR] EndTime Earlier than StartTime
  type: graphql
  seq: 4
}

post {
  url: {{gqlUrl}}
  body: graphql
  auth: none
}

body:graphql {
  mutation UpdateContest(
    $contestId: Int!,
    $input: UpdateContestInput!
  ) {
    updateContest(
      contestId: $contestId
      input: $input
    ) {
      id
    }
  }
}

body:graphql:vars {
  {
    "contestId": 19,
    "input": {
      "startTime": "2030-01-02T00:00:00.000Z",
      "endTime": "2030-01-01T00:00:00.000Z"
    }
  }
}

script:pre-request {
  await require("./login").loginContestAdmin(req);
}

docs {
  # 📙 [ERR] EndTime Earlier than StartTime

  **POST** `/graphql (mutation: updateContest)`

  The start time must be earlier than the end time.

  등록 마감 시간이 시작 시간보다 늦거나 같을 때,

  `UnprocessableDataException`이 발생합니다.
}
