meta {
  name: [ERR] Nonexistent Contest
  type: graphql
  seq: 2
}

post {
  url: {{gqlUrl}}
  body: graphql
  auth: none
}

body:graphql {
  mutation DeleteContest($contestId: Int!) {
    deleteContest(contestId: $contestId) {
      id
    }
  }
}

body:graphql:vars {
  {
    "contestId": 999
  }
}

docs {
  # 📙 [ERR] Nonexistent Contest

  **POST** `/graphql (mutation: deleteContest)`

  The requested contest does not exist.

  존재하지 않는 `contestId`의 대회 삭제를 요청했을 때,

  `EntityNotExistException`이 발생합니다.
}
