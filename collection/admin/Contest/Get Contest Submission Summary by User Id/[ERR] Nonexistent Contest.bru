meta {
  name: [ERR] Nonexistent Contest
  type: graphql
  seq: 3
}

post {
  url: {{gqlUrl}}
  body: graphql
  auth: none
}

body:graphql {
  query GetContestSubmissionSummaryByUserId($contestId: Int!, $userId: Int!, $take: Int!) {
    getContestSubmissionSummaryByUserId(contestId: $contestId, userId: $userId, take: $take) {
      submissions {
        id
      }
    }
  }
}

body:graphql:vars {
  {
    "contestId": 999,
    "userId": 1,
    "take": 10
  }
}

docs {
  # 📙 [ERR] Nonexistent Contest

  **POST** `/graphql (query: getContestSubmissionSummaryByUserId)`

  Returns empty data when the requested contest does not exist.

  존재하지 않는 `contestId`로 조회를 요청했을 때,

  `submissions`를 빈 배열(`[]`)로 반환합니다.
}
