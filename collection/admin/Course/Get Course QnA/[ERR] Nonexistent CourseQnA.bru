meta {
  name: [ERR] Nonexistent CourseQnA
  type: graphql
  seq: 2
}

post {
  url: {{gqlUrl}}
  body: graphql
  auth: none
}

body:graphql {
  query GetCourseQnA($groupId: Int!, $order: Int!) {
    getCourseQnA(groupId: $groupId, order: $order) {
      id
    }
  }
}

body:graphql:vars {
  {
    "groupId": 2,
    "order": 999
  }
}

assert {
  res.body.errors: isDefined
}

script:pre-request {
  await require("./login").loginInstructor(req);
}

docs {
  # 📙 [ERR] Nonexistent CourseQnA

  **POST** `/graphql (query: getCourseQnA)`

  The requested CourseQnA does not exist.

  존재하지 않는 `order`로 Q&A 조회를 요청했을 때,

  `EntityNotExistException('CourseQnA')`이 발생합니다.
}
