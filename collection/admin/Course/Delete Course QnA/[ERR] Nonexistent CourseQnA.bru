---

### **파일: `4. Delete Course QnA/[ERR] Nonexistent CourseQnA.bru`**
```bruno
meta {
  name: [ERR] Nonexistent CourseQnA
  type: graphql
  seq: 2
}

post {
  url: {{gqlUrl}}
  body: graphql
  auth: none
}

body:graphql {
  mutation DeleteCourseQnA($groupId: Int!, $order: Int!) {
    deleteCourseQnA(groupId: $groupId, order: $order) {
      id
    }
  }
}

body:graphql:vars {
  {
    "groupId": 2,
    "order": 999
  }
}

script:pre-request {
  await require("./login").loginInstructor(req);
}

docs {
  # 📙 [ERR] Nonexistent CourseQnA

  **POST** `/graphql (mutation: deleteCourseQnA)`

  The requested QnA does not exist in the course.

  존재하지 않는 `order`로 Q&A 삭제를 요청했을 때,

  `EntityNotExistException('CourseQnA')`이 발생합니다.
}
