meta {
  name: Succeed
  type: graphql
  seq: 1
}

post {
  url: {{gqlUrl}}
  body: graphql
  auth: none
}

body:graphql {
  mutation DeleteCourseQnA($groupId: Int!, $order: Int!) {
    deleteCourseQnA(groupId: $groupId, order: $order) {
      id
      order
      title
      content
      createTime
    }
  }
}

body:graphql:vars {
  {
    "groupId": 2,
    "order": 1
  }
}

assert {
  res.body.data.deleteCourseQnA: isDefined
}

script:pre-request {
  await require("./login").loginInstructor(req);
}

docs {
  # 📘 Delete Course QnA

  **POST** `/graphql (mutation: deleteCourseQnA)`

  Deletes a course QnA.

  코스 Q&A를 삭제하고, 삭제된 객체를 반환합니다.

  ---

  ### 🔒 Authentication

  ✅ Required (Course Staff)

  ---

  ### 📥 Request Variables

  - `groupId` (Int, required): 코스 ID
  - `order` (Int, required): 삭제할 Q&A 순번
}
