meta {
  name: CONFLICT
  type: graphql
  seq: 2
}

post {
  url: {{gqlUrl}}
  body: graphql
  auth: none
}

body:graphql {
  mutation CreateTags($tagNames: [String!]!) {
    createTags(tagNames: $tagNames) {
      id
      name
      createTime
      updateTime
    }
  }
}

body:graphql:vars {
  {
    "tagNames": ["If Statement"]
  }
}

assert {
  res.body.errors[0].extensions.code: eq CONFLICT
  res.body.errors[0].message: eq tag is already in use
}
