meta {
  name: Success
  type: graphql
  seq: 1
}

post {
  url: {{gqlUrl}}
  body: graphql
  auth: none
}

body:graphql {
  query {
    getUsers {
      id
      username
      role
      email
      lastLogin
      createTime
      updateTime
    }
  }
}

script:pre-request {
  await require("./login").loginAdmin(req);
}
