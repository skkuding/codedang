apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: redis
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: redis
    server: https://kubernetes.default.svc
  source:
    repoURL: https://github.com/skkuding/codedang
    path: apps/k8s/redis
    targetRevision: t1937-migrate-redis-to-on-premise-k8s
    helm:
      valueFiles:
        - values.yaml
        - values-prod.yaml
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
