architecture: replication

auth:
  enabled: false
  # existingSecret: "" # TODO: Sealed Secret을 통해 추가

replica:
  replicaCount: 2

sentinel:
  enabled: true

# TODO: 모니터링 설정 추가
# metrics:
#   enabled: true
#   serviceMonitor:
#     enabled: true
#     additionalLabels:
#       "service.name": "redis"
#       "service.environment": "production"
#   podMonitor:
#     enabled: true
#     additionalLabels:
#       "service.name": "redis"
#       "service.environment": "production"
