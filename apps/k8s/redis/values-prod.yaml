architecture: replication

auth:
  enabled: false
  # existingSecret: "" # TODO: Sealed Secret을 통해 추가

master:
  count: 1

replica:
  replicaCount: 2

sentinel:
  enabled: true

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
    additionalLabels:
      "service.name": "redis"
      "service.environment": "production"
  podMonitor:
    enabled: true
    additionalLabels:
      "service.name": "redis"
      "service.environment": "production"
