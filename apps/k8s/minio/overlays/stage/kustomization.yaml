apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: minio

resources:
  - ../../base
  - tenant-config.yaml
  - tenant.yaml
  - users.yaml

patches:
  - path: ingress.yaml
    target:
      kind: Ingress
      name: minio-console-ingress
