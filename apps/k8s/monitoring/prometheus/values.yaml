grafana:
  enabled: false
# (선택) Alertmanager도 별도로 관리하거나 사용하지 않는다면 비활성화할 수 있습니다.
# alertmanager:
#   enabled: false

# (선택) Node Exporter나 kube-state-metrics 등도 필요 없다면 비활성화 가능합니다.
# kube-state-metrics:
#   enabled: false
# nodeExporter:
#   enabled: false
