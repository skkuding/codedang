apiVersion: v1
kind: ConfigMap
metadata:
  name: admin-api-env
  namespace: admin-api
data:
  NODEMAILER_FROM: 'Codedang <noreply@codedang.com>'
  # TODO: uncomment when jplag is needed
  # RABBITMQ_API_URL: 'http://rabbitmq.rabbitmq.svc.cluster.local:15672/api'
  # RABBITMQ_DEFAULT_VHOST: 'vh'
  # RABBITMQ_HOST: 'rabbitmq.rabbitmq.svc.cluster.local'
  # RABBITMQ_PORT: '5672'
  REDIS_HOST: null # need to be set in overlays
  REDIS_PORT: '6379'
  TESTCASE_BUCKET_NAME: 'codedang-testcase'
  MEDIA_BUCKET_NAME: 'codedang-media'

  # TODO: connect after observability services are implemented
  # OTEL_EXPORTER_OTLP_ENDPOINT_URL: 'otel-collector.observability.svc.cluster.local:4317'
